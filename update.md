## Mobile app updates
moble app should have register and sign in screens first.because I need to move the qr authentication to the machines authentication.
User cann register with a new account or sign in then user can access the app.
then there should be 3 bottom tabs
    1. chats = this contain all the chats(same as now) with the harnesses with the approval requests cards just like now.then user can interactively scroll and see the resoning approve the requests prompt just like a chat.
    in the top of that chat screent there should be filter chips for machines.user can switch between sessions of the machines.
    2. machines = this is where the registered machines display if user need to connect a machine there should be button to scan the Qr and connect it user should be able to delete the machines as well.(if machine already registered it should notify too)
    3. profile = this is where the the registered user profiles dislay.

## dekstop app updates
dektop app also should have the register and sign in screens. just like now if this machine already registered it should show just like now.in the screen QR should only show whe the machine is not connected with any mobile device. if machine connected with a mobile device that device should show there.user should be able to remove the connected mobile and connect to another mobile.when removed the QR again should show to connect with a device